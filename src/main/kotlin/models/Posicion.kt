package org.example.models

/**
 * Enum class que representa las opciones del atributo posición de un jugador.
 * @see [Jugador]
 */
enum class Posicion {
    PORTERO, DEFENSA, CENTROCAMPISTA, DELANTERO
}